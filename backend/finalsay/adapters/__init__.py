"""Institution adapters (module 1, design.md section 4).

Each adapter is an :class:`InstitutionAdapter` that reads its institution's
fixture file and returns raw notices. Adapters are independently replaceable
behind the ABC.
"""
